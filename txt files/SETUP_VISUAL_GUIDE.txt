╔════════════════════════════════════════════════════════════════════════════╗
║                    FIREBASE SETUP - VISUAL GUIDE                           ║
╚════════════════════════════════════════════════════════════════════════════╝

STEP 1: GO TO FIREBASE CONSOLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Visit: https://console.firebase.google.com/

You should see your projects:
┌─────────────────────────────────────┐
│  Projects                           │
├─────────────────────────────────────┤
│  ☕ shazam-coffee  ← CLICK THIS      │
│  □ other-project                    │
└─────────────────────────────────────┘

Click "shazam-coffee" ↓


STEP 2: NAVIGATE TO REALTIME DATABASE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Left sidebar menu:
┌─────────────────────────────────────┐
│  Build                              │
│  ├─ Authentication                  │
│  ├─ Realtime Database  ← CLICK THIS │
│  ├─ Firestore Database              │
│  ├─ Storage                         │
│  └─ ...                             │
└─────────────────────────────────────┘

Click "Realtime Database" ↓


STEP 3: CLICK THE "RULES" TAB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

At the top of the database view:
┌──────────────────────────────────────┐
│  [Data]  [Rules]  [Backups]          │
│           ↑ CLICK THIS                │
└──────────────────────────────────────┘

Click "Rules" tab ↓


STEP 4: DELETE OLD CODE & PASTE NEW CODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

You'll see an editor with code. DELETE everything.

Before:
┌────────────────────────────────────────┐
│ {                                      │
│   "rules": {                           │
│     ".read": "auth != null",           │  DELETE
│     ".write": "auth != null"           │  ALL
│   }                                    │  THIS
│ }                                      │
└────────────────────────────────────────┘

After (empty):
┌────────────────────────────────────────┐
│                                        │
│                                        │  Now paste the code below ↓
│                                        │
│                                        │
└────────────────────────────────────────┘


STEP 5: PASTE THE CODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copy this ENTIRE code (including { and }):

{
  "rules": {
    "orders": {
      ".read": true,
      ".write": true,
      "$orderId": {
        ".validate": "newData.hasChildren(['id', 'itemName', 'customerName', 'customerPhone', 'orderTime', 'endTime', 'status'])"
      }
    }
  }
}

Paste into the editor:
┌────────────────────────────────────────┐
│ {                                      │
│   "rules": {                           │
│     "orders": {                        │
│       ".read": true,                   │
│       ".write": true,                  │
│       "$orderId": {                    │
│         ".validate": "newData.hasC...  │
│       }                                │
│     }                                  │
│   }                                    │
│ }                                      │
└────────────────────────────────────────┘


STEP 6: CLICK PUBLISH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

At the top right, you'll see a blue button:
┌─────────────────────────────────┐
│  [Cancel]  [Publish]            │
│            ↑ CLICK THIS (BLUE)   │
└─────────────────────────────────┘

Click "Publish" button ↓


STEP 7: WAIT FOR CONFIRMATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Wait 1-2 seconds. You should see a success message:
┌─────────────────────────────────┐
│  ✅ Rules published successfully │
└─────────────────────────────────┘

If you see this, you're done! ✅


OPTIONAL: VERIFY IT WORKED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Click the "Data" tab to see the database:
┌──────────────────────────────────────┐
│  [Data]  [Rules]  [Backups]          │
│   ↑ CLICK THIS                       │
└──────────────────────────────────────┘

You should see:
┌──────────────────────────────────────┐
│  shazam-coffee                       │
│  └─ orders                           │  Empty for now (will fill with orders)
│     └─ (empty - will show orders)    │
└──────────────────────────────────────┘

Perfect! Setup is complete. ✅


COMMON ISSUES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Issue: "I don't see a Publish button"
→ You might still be in "Data" tab
→ Click "Rules" tab at the top

Issue: "The code won't paste"
→ Make sure editor is active (click inside)
→ Clear old code first (Ctrl+A, Delete)
→ Then paste

Issue: "Permission denied error when testing"
→ Rules weren't published
→ Go back to Rules tab
→ Check code is correct
→ Click Publish again
→ Wait 2-3 seconds
→ Refresh the app page

Issue: "I see a red error in the editor"
→ Check for missing comma or bracket
→ Make sure you copied the entire code block
→ Compare with the code above

═══════════════════════════════════════════════════════════════════════════════

ONCE SETUP IS DONE:

1. Open menu.html on your PHONE
2. Place a coffee order
3. Open barista.html on your LAPTOP
4. Login (credentials provided separately)
5. YOU SHOULD SEE THE ORDER APPEAR INSTANTLY! ✅

If it works, you're all set! 🎉

═══════════════════════════════════════════════════════════════════════════════

Need help? Read:
- START_HERE.md
- QUICK_START.txt
- INSTALLATION.md

Made by AJM Digital Solution
